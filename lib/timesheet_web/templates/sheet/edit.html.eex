<h4>Edit Timesheet Status</h4>

<%= render "approval.html", Map.put(assigns, :action, Routes.sheet_path(@conn, :update, @sheet)) %>

<span><%= link "Back", to: Routes.sheet_path(@conn, :index) %></span>
